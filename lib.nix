{
    key = k: a: {
      key = k;
      action = "<cmd>${a}<CR>";
      # mode = [ "n" "v" ];
    };

    keyI = k: a: {
      key = k;
      action = "<cmd>${a}<CR>";
      mode = [ "i" ];
    };
}
